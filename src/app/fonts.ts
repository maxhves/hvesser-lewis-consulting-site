import { Inter, Outfit } from 'next/font/google'

//region Outfit

export const outfit = Outfit({
  subsets: ['latin'],
  display: 'swap',
})

//endregion

//region Inter

export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
})

//endregion
